<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.quantandroid.bingo.stu">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">

        <activity android:name=".thread.ThreadTestActivityDemo_1">

        </activity>

        <activity android:name=".thread.ThreadTestActivityDemo_2">

        </activity>

        <activity android:name=".thread.AsyncTaskActivityDemo_3">

        </activity>

        <activity android:name=".net.OKHttpActivityDemo_4">
            <intent-filter >
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".net.GsonActivityDemo_5"></activity>

    </application>

</manifest>
